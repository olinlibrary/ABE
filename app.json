{
  "name": "ABE",
  "scripts": {
    "postdeploy": "python -m abe.postdeploy",
    "pr-predestroy": "python -m abe.pr-predestroy"
  },
  "env": {
    "FLASK_DEBUG": {
      "description": "Enable Flask debug mode.",
      "value": "False",
      "required": true
    },
    "HEROKU_APP_NAME": {
      "required": true
    },
    "MONGO_URI": {
      "description": "The connection URI for MongoDB.",
      "required": true
    },
    "REQUIRE_SSL": {
      "description": "Use HSTS to redirect to HTTPS.",
      "value": "False",
      "required": true
    }
  },
  "formation": {
  },
  "addons": [],
  "buildpacks": [
    {
      "url": "heroku/python"
    }
  ]
}
